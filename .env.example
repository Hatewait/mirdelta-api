# Пример .env файла. Скопируй этот файл в ".env" и подставь свои значения.

DATABASE_URL="file:./prisma/dev.db"  # путь к БД (локально SQLite, на проде — свой URL)
PORT=4000                            # порт, на котором запускается сервер
CORS_ORIGIN=http://localhost:5173    # адрес фронтенда (React)
